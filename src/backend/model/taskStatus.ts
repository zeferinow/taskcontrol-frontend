export type TaskStatus = 'notStarted' | 'inProgress' | 'waiting' | 'concluded';

export const TaskStatus = {
    NotStarted: 'notStarted' as TaskStatus,
    InProgress: 'inProgress' as TaskStatus,
    Waiting: 'waiting' as TaskStatus,
    Concluded: 'concluded' as TaskStatus
};

;
